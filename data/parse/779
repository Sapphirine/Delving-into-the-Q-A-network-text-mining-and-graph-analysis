train data need one set data sampl categori tri think creat model fed data given thought model might use ? m ask pure classif model achiev better fit amount data class pretti uniform train data howev regress model need data type proport match expect input